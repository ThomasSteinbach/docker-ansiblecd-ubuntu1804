- name: simple test playbook
  hosts: localhost
  tasks:

    - name: install docker
      apt:
        name: docker.io
        state: present

    - name: start docker
      service:
        name: docker
        state: started

    - name: test docker
      command: docker ps
      register: result

    - name: log docker execution
      debug:
        var: result.stdout

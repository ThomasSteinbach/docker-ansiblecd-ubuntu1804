- name: simple test playbook
  hosts: localhost
  tasks:

    - name: test docker
      command: docker ps
      register: result

    - name: log docker execution
      debug:
        var: result.stdout
